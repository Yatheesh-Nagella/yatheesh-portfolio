# OneLibro Environment Variables
# Copy this file to .env.local and fill in your actual values

# ============================================
# SUPABASE
# ============================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL (e.g., https://abcdefgh.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous/public key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Your Supabase service role key (SERVER-SIDE ONLY - keep secret!)
# This bypasses Row Level Security - only use in API routes
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# PLAID
# ============================================
# Get these from: https://dashboard.plaid.com/developers/keys

# Your Plaid client ID
PLAID_CLIENT_ID=your_plaid_client_id

# Your Plaid secret (matches your environment - keep secret!)
PLAID_SECRET=your_plaid_secret

# Plaid environment: sandbox | development | production
# - sandbox: Test mode with fake data (no real bank connections)
# - development: Live mode with limited functionality (free)
# - production: Full live mode (requires approval)
PLAID_ENV=sandbox

# Optional: Webhook URL for Plaid events (transaction updates, etc.)
# In production: https://finance.yourdomain.com/api/plaid/webhook
# In development: Use ngrok or similar tunneling service
# PLAID_WEBHOOK_URL=https://your-domain.com/api/plaid/webhook

# ============================================
# ENCRYPTION
# ============================================
# 32-byte (64 hex characters) encryption key for sensitive data
# Generate with: openssl rand -hex 32
# Used for encrypting Plaid access tokens and admin TOTP secrets
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ============================================
# APPLICATION
# ============================================
# Your application's base URL (used for redirects, emails, etc.)
# In development: http://localhost:3000
# In production: https://yatheeshnagella.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# NOTES
# ============================================
# - Never commit .env or .env.local to git
# - Keep SUPABASE_SERVICE_ROLE_KEY and PLAID_SECRET secure
# - Use different Plaid keys for development and production
# - Rotate ENCRYPTION_KEY if compromised (will invalidate existing encrypted data)
# - For local development, copy this file to .env.local and fill in real values
